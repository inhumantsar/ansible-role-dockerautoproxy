version: "3.3"

services:
    nginx:
        image: jwilder/nginx-proxy:{{ nginx_version }}
        ports:
            - 80:80
        networks:
            - proxy_net
        volumes:
            - /var/run/docker.sock:/tmp/docker.sock:ro
        deploy:
            placement:
                constraints:
                    - node.role == manager

networks:
    proxy_net:
        driver: overlay
